{
  "name": "memory-graph-prototype",
  "version": "0.1.0",
  "description": "Associative memory graph system — inhabited knowing for conversational AI",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "sleep": "node -e \"require('./src/sleep').runSleepCycle()\"",
    "reset-db": "node -e \"require('./src/db').resetDatabase()\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true,
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "better-sqlite3-multiple-ciphers": "^11.0.0",
    "dotenv": "^16.4.0",
    "node-cron": "^3.0.3",
    "uuid": "^11.0.0"
  }
}
